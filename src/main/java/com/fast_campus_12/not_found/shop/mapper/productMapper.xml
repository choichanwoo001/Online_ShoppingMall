<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fast_campus_12.not_found.shop.product.repository.ProductMapper">

    <select id="findById" resultType="com.fast_campus_12.not_found.shop.product.model.Product">
        SELECT
            product_id,
            lv3_id,
            title,
            price,
            discount_price,
            summary,
            description,
            thumbnail,
            weight,
            dimensions,
            tags,
            seasons,
            average_rating,
            sales_count,
            view_count,
            stock_quantity,
            availability_date,
            enabled,
            created_at,
            updated_at,
            deleted_at,
            is_deleted
        FROM product
        WHERE product_id = #{productId}
    </select>

</mapper>
